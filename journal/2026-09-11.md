# 2026-09-11（周五）

**今日投入**：__ 小时

## 做了什么
- 建起 GitHub 记录系统（本地仓库 + 远程 + 首次推送）
- 默写 `attention_blank.py` 第 1 次：__ 分钟，__ / 9
- 力扣 D1 · 1. 两数之和（简单）
- **跑通 HuggingFace official quickstart**（`week01/hf_quickstart.py`），含三个观察实验
- 精读《Attention Is All You Need》，摘要存 `papers/01-attention-is-all-you-need.md`
- 给王老师发消息问组内 GPU / 工作站

## 卡点 / 报错
- **huggingface.co 连接超时**（10s 无响应）→ 改用 `HF_ENDPOINT=https://hf-mirror.com`
- **Xet 后端下大文件超时**（`cas-bridge.xethub.hf.co: The read operation timed out`）
  → 该域名不经过镜像，加 `HF_HUB_DISABLE_XET=1`；首次下载共耗 12 分钟
- 默认缓存在 C 盘（只剩 24GB）→ 改到 `E:\hf_cache`

## 结论
- 注意力机制已能默写（9/9）
- `attention_mask` 不能省：不传时单条与批量推理的 logits 差 **7.4e-02**（传了只差 7.2e-07）
- 英文词表下中文会大量变 `[UNK]`（实测 103 字符里 19 个），中文任务必须换词表

## 明天
- 
