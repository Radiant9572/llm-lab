"""
49. 字母异位词分组   [中等]
日期：2026-09-12   用时：15 分钟   限时：15 分钟   结果：看题解后做出

关键洞察：
    思路卡在"怎么把排序后相同的字符串聚到同一个列表里"。
    关键那一步是：**把 sorted(s) 的结果当作 key** —— 于是"同一组异位词"变成"同一个 key"，
    分组问题就退化成普通的字典分组。这一步想到了，代码是很短的。

复杂度：
    我的解法：O(n · k log k) 时间 / O(n · k) 空间
              n = 单词个数，k = 最长单词长度；每个词排序要 k log k，共 n 个词
    另一种解法：用 26 个字母的计数当 key，复杂度 O(n · k)，省掉 log k
              （D9 · 438 题会用这个套路）
    差距在哪：说明这题的本质是"给一组元素找一个规范形式当 key"，
              选什么当 key 直接决定复杂度

踩坑：
    sorted(s) 返回的是**列表**，列表不可哈希，不能直接当 dict 的 key。
    必须 ''.join(...) 拼回字符串。（详见文末 ps）
"""

from typing import List


# ============================================================================
# ↓ 这版是参考题解，不是我独立写出来的
#   所以 docstring 的「结果」栏保持"看题解后做出"，不要改成"独立做出"
#
#   重写打卡：____ 月 ____ 日   用时 ____ 分钟   不看题解独立写完 ☐
#   打卡通过后，才把结果栏改成"独立做出"，并补一句"第一次卡在哪"
# ============================================================================
class Solution:

    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:

        d = {}  #

        for s in strs: #ps：我经常会写for i in range(len(strs)), 然后用strs[i]来表示，但是这样太麻烦了，要改正
            sorted_s = ''.join(sorted(s))  # 把 s 排序，作为 dict 的 key
# ps：我知道sorted方法，但是不知道这样会得到一个列表，所以还需要''.join()
            if sorted_s not in d:  # 首次遇到 sorted_s

                d[sorted_s] = []  # 创建列表
# ps：这是我想不到也是困住我的一句代码，我不知道将排序后相同的字符串聚在同一个列表中
            d[sorted_s].append(s)  # 排序后相同的字符串，保存到同一组中
#ps：同理，我想不到这一句代码，因为我之前根本没有见过
        return list(d.values())  # 哈希表的所有 value 就是分组结果


# ============================================================================
# 你"想不到"的那两句：
#
#     if sorted_s not in d:      <- 判断键在不在
#         d[sorted_s] = []       <- 建空列表
#     d[sorted_s].append(s)      <- 追加
#
# 这三行合起来，用 collections.defaultdict(list) 一行就够：
#     dd[sorted_s].append(s)     # 键不存在时自动建空列表，不用先判断
#
# 去做 python_drill.py 的 ex11 / ex12（5 分钟两道），
# 做完回来把上面这个 groupAnagrams 用 defaultdict 改写一遍 —— 这就是"补词汇"。
# ============================================================================
