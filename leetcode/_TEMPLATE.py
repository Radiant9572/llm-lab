"""
========================================================================
刷题记录模板 —— 复制这个文件，改名为  题号_英文slug.py
例如：0001_two_sum.py / 0042_trapping_rain_water.py

复制后：
  1. 删掉所有 ★ 开头的行（那些是填写指引，不留在成品里）
  2. 填 docstring 里的四个字段
  3. 贴你的代码
========================================================================


★ 下面这段是「第 3 题」的示例记录，只用来看**格式长什么样**。
★ 不要抄它的内容 —— 那是另一道题的答案。
★ 今天犯过的错就是照着示例抄了一段和题目无关的注释。

------------------------------------------------------------------------
3. 无重复字符的最长子串   [中等]
日期：2026-09-15   用时：22 分钟   限时：25 分钟   结果：看题解后做出

关键洞察：
    窗口内必须是"无重复"的，所以右扩到重复时，左边界要跳到
    该重复字符上次出现位置的下一位 —— 而不是一格一格往右挪。
    "跳"而不是"挪"，是 O(2n) 变成 O(n) 的原因。

复杂度：
    我的解法：O(n²) 时间 / O(k) 空间 —— 左指针一格一格挪
    最优解法：O(n) 时间   / O(k) 空间
    差距在哪：用 dict 记录"字符 → 最后出现的下标"，左指针可以一步跳过去

踩坑：
    左指针收缩时要从集合里真正删掉元素，不能只移动下标。
    （我的第一版只挪了下标，导致窗口内容整个是错的）
------------------------------------------------------------------------


★ 四个字段怎么才叫写好：

  【关键洞察】
    写"让你豁然开朗的那一点"，不是写步骤。
     ✗ 差："用哈希表做"            ← 只说了方法名，没说为什么
     ✓ 好："内层循环在找 target-x，把这件'找'从遍历变成 O(1) 查询"
    自检：把这句话给一个没做过这题的人看，他能不能立刻明白该怎么做

  【复杂度】
    必须分两行：**我的解法** 和 **最优解法**。
    只写最优解是自我欺骗 —— 面试时你写出来的是"我的解法"。
    估计不出来也要写，写错比不写强（写错至少知道你不会）

  【踩坑】
    写"我原来错在哪"，没有就写"无"。
    ✗ 差："注意边界条件"          ← 这是废话，谁都知道
     ✓ 好："内层循环从 i+1 开始，从 0 开始会把同一个元素用两次"
    自检：这条能不能对应到 README 里的错误类型分类表？

  【结果】
    四选一，如实写：独立做出 / 看题解后做出 / 看题解才懂 / 没做出来
    "看题解后做出"不是耻辱 —— 一周后能独立做出才是目标。
    但**如实记录**是前提，不然二刷时你不知道该重点看哪几道
========================================================================
"""

from typing import List


class Solution:
    def solve(self, *args):
        """★ 贴你的代码到这里，并把函数签名改成题目要求的名字。"""
        raise NotImplementedError
