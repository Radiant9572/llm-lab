# 2026-09-12（周六）· 执行清单

**定位**：W1 的收尾日。**总投入约 3 小时**，可以整块用，也可以拆成上午/下午两段。

今天不用开新东西——把昨天剩下的四件事清掉，周一就能干净地进 W2（手写完整 GPT）。

---

## ① 推送到 GitHub · 3 分钟

昨天那两个提交还卡在本地（我这边推 GitHub 的通道挂了）：

```powershell
cd E:\llm-lab
git fetch origin
git push
```

- [ ] 确认 `git status` 显示 `## main...origin/main`（不再有 `[gone]`）
- [ ] 刷新仓库页面，能看到最新提交

**3 分钟的事，但别拖。** 记录体系靠的是"每天都有提交"这个习惯，断一天就有第二天。

---

## ② 补写 0001 的哈希表版本 · 10 分钟

昨天你写的"可以利用哈希表来找，但代码我不会写"——缺口补上就 10 分钟，而且**这题的面试价值全在那个版本上**。

写在 `leetcode/0001_two_sum.py` 同一个文件里，加注释说明思路。动手之前先回答一个问题：

> **哈希表应该"先查再存"，还是"先存再查"？**
> 用 `nums = [3, 3], target = 6` 把两种顺序各推一遍，看哪个会出错。

推出来了再写代码。写完对比一下：复杂度从 O(n²) 降到 O(n)，代价是多花了 O(n) 空间——**你已经把这个权衡写进记录了**。

---

## ③ 读论文 · 105 分钟（今天的重点）

材料全部就位：

| 用途 | 文件 |
|---|---|
| **方法 + 导航** | `papers/01-reading-guide.md` ← **先看这个再开始** |
| **中英对照** | `D:\pdf2zh\output\attention-is-all-you-need.zh.dual.pdf` |
| 原文 | `C:\Users\蒙蒙\Desktop\note\NIPS-2017-attention-is-all-you-need-Paper.pdf` |
| 摘要模板 | `papers/01-attention-is-all-you-need.md` |

**唯一要守的规则：英文在前，中文只做校验。** 每段先读英文 → 心里归纳一句 → 再看中文确认 → 猜错了就圈出没认出来的词。

时间按导览里的表走（§3.1 / §3.2 / §3.2.1 / §3.5 精读，其余快扫）。**开计时器，超时往下走，不要卡死在某一节。**

- [ ] 读完 §3.2（缩放点积）—— 这是全文最重要的一节
- [ ] 读完 §3.2.1（多头）—— 直接对应你下周的 W2
- [ ] 填完 `papers/01-attention-is-all-you-need.md` 的四个字段
- [ ] 「我的问题」那栏**不许写"没有"**

---

## ④ 力扣 D2 · 30 分钟

**49. 字母异位词分组（中等）**

- [ ] 限时 **25 分钟**。超时就去看题解，不要硬耗
- [ ] 看完关掉，从空白重写一遍 ← **这步不能省**
- [ ] `cp _TEMPLATE.py 0049_group_anagrams.py`，填四个字段
- [ ] `SCHEDULE.md` 里把 D2 勾上（`- [ ]` → `- [x]`，**只能用小写 x**）

**这题的两个关键**：「异位词」的判定标准是什么；怎么让它们落到同一个桶里。想到了就会做，想不到会卡很久——**卡过 25 分钟就看题解**。

---

## ⑤ 给王老师发消息 · 10 分钟

**这是唯一一件拖了两天、且优先级最高的事。** 直接发，不用挑时间：

> 王老师您好，我是今年入学的大数据专硕 Radiant。上次您提到研一下可以进组，我最近在自学 Transformer，已经手写实现了注意力机制，接下来想训一个千万参数级别的小模型做验证实验。
>
> 想请教一下：**组里现在有没有空闲的 GPU 机器，或者能先给我开个账号？** 我需要的量不大（单卡跑几天就够），主要是想把训练流程跑通。
>
> 另外组里有没有闲置的工作站或服务器可以先借我用一下？谢谢老师！

- [ ] 发出去

**注意别问错问题**：不要问"我能不能提前进组"（行政问题，答案一定是"按培养方案来"）。要问**资源**，而且绑一个具体小任务。

**回什么都无所谓，重点是问清楚"进组前有没有组内算力"这个前提。** 有 → 省下全部租卡预算；没有 → 按 `算力方案与预算.md` 走，也别再追。

---

## ⑥ 日报 · 3 分钟

昨天 `journal/2026-09-11.md` 里还留着两个空：**「今日投入 __ 小时」和「默写 __ 分钟」**（后者是 8 分钟 / 9 分，你已经记在 `attention_blank.py` 里了）。

- [ ] 把 `journal/2026-09-11.md` 的空补上
- [ ] `cp journal/README.md` 看一眼格式，新建 `journal/2026-09-12.md`

**日报 ≤ 10 行。** 三个栏位各一两句就够：

| 栏位 | 写什么 |
|---|---|
| 做了什么 | 具体到能复现 |
| **卡点** | 报错原文 + 你怀疑的原因 + 怎么解的 ← **这栏最值钱** |
| 结论 | 一句能被证伪的话（不要写"很有收获"） |

**为什么值得花这 3 分钟**：三个月后你回头看，最有用的不是"我做了什么"，是"我踩过哪些坑"。面试被追问细节时，那些就是你的弹药。

---

## 今天结束时的验收

- [ ] GitHub 上能看到最新提交
- [ ] 0001 有两个版本（O(n²) 和 O(n)）
- [ ] `papers/01-attention-is-all-you-need.md` 四个字段填满，含「我的问题」
- [ ] D2 完成并勾选
- [ ] 给王老师的消息发出去了
- [ ] 两天的日报没有空栏

**做完这些，W1 就是真正意义的完结** —— 只剩周日 15 分钟的周节奏（更新 `README.md` 进度表 + 检查这周 commit 有没有断档）。

---

## 明天不要做的事

| 不要 | 为什么 |
|---|---|
| 提前开 W2（写完整 GPT） | 留到周一。W2 是新阶段的开始，需要一个干净的起点 |
| 论文读不完就熬夜补 | 读不完就砍：§3.2 + §3.2.1 读完，其余快扫，记录里注明哪里没读 |
| 把哈希表版本改得"更优雅" | 能跑就行。你的时间是稀缺资源 |
| 一次刷两道力扣 | D2 一道就够，明天周日还有 D3 |

---

*配套：`papers/01-reading-guide.md`（论文怎么读）、`leetcode/README.md`（记录格式）、`PLAN.md`（26 周总览）*
